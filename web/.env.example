# FastAPI backend (same machine, default port)
BACKEND_URL=http://localhost:8080

# SQLite database path (relative to web/ or absolute)
DATABASE_PATH=../data/db.sqlite

# Session JWT secret — generate with: openssl rand -hex 32
SESSION_SECRET=replace_me_with_32_random_chars
